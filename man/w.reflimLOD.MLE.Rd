% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reflim_Weighted_Sliding.R
\name{w.reflimLOD.MLE}
\alias{w.reflimLOD.MLE}
\title{Estimates the reference interval based on a maximum likelihood approach with weighting}
\usage{
w.reflimLOD.MLE(
  measured.values,
  lod,
  n.lod,
  weights = NULL,
  lambda = 0,
  right.quantile = 0.75,
  verbose = FALSE
)
}
\arguments{
\item{measured.values}{The measured values above LOD.}

\item{lod}{Limit of Detection (LOD).}

\item{n.lod}{The number of values below LOD.}

\item{weights}{Weights}

\item{lambda}{The lambda value for the Box-Cox transformation leading to a normal distribution of the values from the healthy subjects.}

\item{right.quantile}{The (iterated) estimation of the spread is based on the median and the quantile specified by right.quantile. The default is the third quartile.}

\item{verbose}{If TRUE, prints additional information during execution.}
}
\value{
A list with the following elements:
\describe{
\item{lower.limit}{The estimate for the lower limit of the reference interval. If the
assumption that the median of the values from the healthy subjects is
above LOD is not fulfilled, NA is returned.}
\item{upper.limit}{The estimate for the upper limit of the reference interval. If the
assumption that the median of the values from the healthy subjects is
above LOD is not fulfilled, NA is returned.}
\item{percentile1}{The estimate for the 1\\% quantile of the values from the healthy subjects.
If the assumption that the median of the values from the healthy
subjects is above LOD is not fulfilled, NA is returned.}
\item{percentile99}{The estimate for the 99\\% quantile of the values from the healthy
subjects. If the assumption that the median of the values from the
healthy subjects is above LOD is not fulfilled, NA is returned.}
\item{mu.log}{The estimate for the expected value of the normal distribution of the
Box-Cox transformed values from the healthy subjects. If the assumption
that the median of the values from the healthy subjects is above LOD is
not fulfilled, NA is returned.}
\item{sigma.log}{The estimate for the standard deviation of the normal distribution of
the Box-Cox transformed values from the healthy subjects. If the
assumption that the median of the values from the healthy subjects is
above LOD is not fulfilled, NA is returned.}
\item{upper.truncation}{Estimate for the 97.5\\%-quantile of the healthy population. If the
assumption that the median of the values from the healthy subjects is
above LOD is not fulfilled, NA is returned.}
\item{selected.values}{The subset of values from measured.values that are assumed to be below
the 97.5\\%-quantile of the healthy population. If the assumption that the
median of the values from the healthy subjects is above LOD is not
fulfilled, NA is returned.}
\item{minus.log.likelihood}{Negative log-likelihood.}
\item{lod}{LOD as it was specified in the argument of this function.}
\item{n.lod}{The number of values below LOD as it was specified in the argument of
this function.}
\item{lambda}{Lambda Value.}
}
}
\description{
Removes outliers from a vector of measured values with values below LOD and with weights and estimates the
reference interval based on a maximum likelihood approach. It is assumed that the values
from the healthy subjects follow a  normal distribution after the application of
pre-specified Box-Cox transformation. The default is lambda=0, i.e. it is assumed that
the values from the healthy subjects follow a lognormal distribution. It is also assumed
that the median of the values from the  healthy subjects is above LOD.
}
