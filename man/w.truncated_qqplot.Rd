% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reflim_Weighted_Sliding.R
\name{w.truncated_qqplot}
\alias{w.truncated_qqplot}
\title{Quantile-Quantile plot of truncated laboratory results with weights}
\usage{
w.truncated_qqplot(
  x.trunc,
  x_weight,
  lognormal = NULL,
  perc.trunc = 2.5,
  n.min = 200,
  apply.rounding = TRUE,
  plot.it = FALSE,
  main = "w.Q-Q plot",
  xlab = "theoretical quantiles",
  ylab = "sample quantiles"
)
}
\arguments{
\item{x.trunc}{Truncated numeric vector of positive values, usually generated by \code{iboxplot}.}

\item{x_weight}{Weights of the numeric vector of positive numbers.}

\item{lognormal}{Logical. Indicates whether a lognormal distribution should be assumed. If \code{NULL}, the type is determined automatically.}

\item{perc.trunc}{Numeric. Percentage of values that were removed from each side by truncation. Default is \code{2.5}.}

\item{n.min}{Integer. Minimum number of values required in \code{x.trunc} for a robust estimate. Default is \code{200}.}

\item{apply.rounding}{Logical. If \code{TRUE}, the reference limits are rounded. Default is \code{TRUE}.}

\item{plot.it}{Logical. If \code{TRUE}, a Q-Q plot is drawn. Default is \code{TRUE}.}

\item{main}{Character. Title of the plot. Default is \code{"Q-Q plot"}.}

\item{xlab}{Character. Label for the x-axis. Default is \code{"theoretical quantiles"}.}

\item{ylab}{Character. Label for the y-axis. Default is \code{"sample quantiles"}.}
}
\value{
A list with the following components:
\describe{
\item{result}{A numeric vector containing the intercept and slope of the fitted Q-Q line, and the estimated lower and upper truncation points (reference limits).}
\item{lognormal}{Logical. Indicates whether a lognormal distribution was assumed.}
}
}
\description{
Generates and plots a quantile-quantile plot (Q-Q plot) with a theoretical normal distribution on the x-axis
and the corresponding empirical distribution on the y-axis. Returns the intercept, slope, and estimated
2.5\% and 97.5\% quantiles (i.e., preliminary reference limits).
}
