% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{synthetic_data}
\alias{synthetic_data}
\title{Generate Synthetic Laboratory Data from Subgroups with Limits}
\usage{
synthetic_data(
  n = c(100, 800, 100),
  ll = c(10, 12, 15),
  ul = c(13, 16, 20),
  lognormal = FALSE,
  hist.bins = 50,
  plot.it = TRUE,
  add.boxplot = TRUE,
  plot.legend = TRUE,
  pos.legend = "topright",
  main = "",
  xlab = "",
  apply.rounding = TRUE,
  digits = NULL
)
}
\arguments{
\item{n}{Integer vector specifying sample sizes for each subgroup.}

\item{ll}{Numeric vector specifying the lower reference limits for each subgroup.}

\item{ul}{Numeric vector specifying the upper reference limits for each subgroup.}

\item{lognormal}{Logical, whether to generate lognormal distribution data instead of normal. Default is \code{FALSE}.}

\item{hist.bins}{Number of histogram bins if sample size > 200. Default is 50.}

\item{plot.it}{Logical, whether to generate a visualization plot. Default is \code{TRUE}.}

\item{add.boxplot}{Logical, whether to superimpose a horizontal boxplot on the top. Default is \code{TRUE}.}

\item{plot.legend}{Logical, whether to display a legend with reference intervals. Default is \code{TRUE}.}

\item{pos.legend}{Position of the legend. Default is \code{"topright"}.}

\item{main}{Title of the plot. Default is \code{""}.}

\item{xlab}{Label for x-axis. Default is \code{""}.}

\item{apply.rounding}{Logical, whether to round the generated data. Default is \code{TRUE}.}

\item{digits}{Integer, number of decimal digits to round. If \code{NULL}, auto-determined.}
}
\value{
A list containing \code{values} and \code{stats}.
}
\description{
Generates synthetic data for multiple subgroups based on reference limits (lower and upper limits),
estimates underlying distribution parameters, and optionally plots histograms, densities, and boxplots.
}
