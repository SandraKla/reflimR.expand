% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpart_analysis.R
\name{generate_rpart}
\alias{generate_rpart}
\title{Generate and Plot Regression Tree for Patient Partitioning}
\usage{
generate_rpart(df, analyte = "ALT", tree_cp = 0.01, tree_minsplit = 30)
}
\arguments{
\item{df}{A data.frame containing analyte values, \code{Age}, and \code{Sex}.}

\item{analyte}{Character string specifying the target analyte column name (default is \code{"ALT"}).}

\item{tree_cp}{Numeric. Complexity parameter for rpart pruning (default is \code{0.01}).}

\item{tree_minsplit}{Integer. Minimum number of observations in a node to attempt a split (default is \code{30}).}
}
\value{
An \code{rpart} object (returned invisibly).
}
\description{
Fits an ANOVA regression tree using \code{rpart} to partition patients by Age and Sex,
and visualizes the resulting tree via \code{rpart.plot}.
}
\examples{
\donttest{
set.seed(123)
sample_data <- data.frame(
  Age = sample(18:80, 200, replace = TRUE),
  Sex = sample(c("m", "f"), 200, replace = TRUE),
  ALT = rnorm(200, mean = 25, sd = 5)
)
fit <- generate_rpart(sample_data, analyte = "ALT", tree_cp = 0.05, tree_minsplit = 20)
}

}
