% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lab_mclust.R
\name{lab.mclust}
\alias{lab.mclust}
\title{Gaussian mixture modelling for laboratory data}
\usage{
lab.mclust(
  x,
  lognormal = FALSE,
  remove.extremes = FALSE,
  targets = NULL,
  plot.it = TRUE,
  add.boxplot = TRUE,
  plot.legend = TRUE,
  pos.legend = "topright",
  plot.bic = FALSE,
  xlim = NULL,
  ylim = NULL,
  main = "",
  xlab = "",
  hist.bins = 50,
  model = NULL,
  n.cluster = NULL,
  apply.rounding = TRUE,
  digits = NULL
)
}
\arguments{
\item{x}{Numeric vector containing positive laboratory measurements.
At least 200 observations are recommended.}

\item{lognormal}{Logical. If \code{TRUE}, the model is fitted to logarithmically
transformed measurements.}

\item{remove.extremes}{Logical. If \code{TRUE}, very large values are removed
before fitting the model.}

\item{targets}{Optional numeric vector containing two target limits to add
to the plot.}

\item{plot.it}{Logical. If \code{TRUE}, diagnostic plots are generated.}

\item{add.boxplot}{Logical. If \code{TRUE}, a boxplot is added to the histogram.}

\item{plot.legend}{Logical. If \code{TRUE}, a legend is added.}

\item{pos.legend}{Position passed to \code{\link[graphics:legend]{graphics::legend()}}.}

\item{plot.bic}{Logical. If \code{TRUE}, the BIC values are plotted.}

\item{xlim}{Optional numeric vector defining the x-axis limits.}

\item{ylim}{Optional numeric vector defining the y-axis limits.}

\item{main}{Main title of the plot.}

\item{xlab}{Label of the x-axis.}

\item{hist.bins}{Number of histogram break points.}

\item{model}{Optional \code{mclust} model name, such as \code{"E"} or \code{"V"}.}

\item{n.cluster}{Optional number or vector of numbers of mixture components.}

\item{apply.rounding}{Logical. If \code{TRUE}, reference limits are rounded.}

\item{digits}{Number of decimal places used for rounding.}
}
\value{
A list containing:
\describe{
\item{n.cluster}{Selected number of mixture components.}
\item{stats}{Estimated limits, proportions, means and standard deviations.}
\item{y.max}{Maximum y-axis value used for plotting, or \code{NULL}.}
\item{BIC}{BIC values returned by \code{\link[mclust:Mclust]{mclust::Mclust()}}.}
}
}
\description{
Uses Gaussian finite mixture models to identify different subpopulations
in laboratory measurement data and estimate component-specific reference
intervals.
}
\examples{
\donttest{
set.seed(123)
values <- c(
  rnorm(120, mean = 20, sd = 2),
  rnorm(120, mean = 40, sd = 3)
)

result <- lab.mclust(
  values,
  lognormal = FALSE,
  remove.extremes = FALSE,
  plot.it = FALSE,
  model = "V",
  n.cluster = 2
)

result$stats
}

}
