% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{make_data}
\alias{make_data}
\title{Generator for Age-Dependent Laboratory Measurements Using Mathematical Trends}
\usage{
make_data(
  age,
  age_steps = 365,
  distribution = "NO",
  n_ = 100,
  name_value = "Synthetic_Analyte",
  formula_mu = "linear(x, 0, 10)",
  formula_sigma = "linear(x, 0, 1)",
  formula_nu = "linear(x, 0, 1)",
  formula_tau = "linear(x, 0, 2)",
  prop.path = 0,
  ill_factor = NULL,
  mu_factor_ill = 0,
  lod = NULL,
  seed = NULL
)
}
\arguments{
\item{age}{Numeric. Maximum age range for simulation (in years).}

\item{age_steps}{Numeric. Step size for data generation in days (e.g., 365 for 1-year steps).}

\item{distribution}{Character. Type of distribution: \code{"NO"} (Normal), \code{"LOGNO"} (Log-Normal),
\code{"BCCG"}, \code{"BCPE"}, or \code{"BCT"}.}

\item{n_}{Integer. Total number of observations per age step.}

\item{name_value}{Character. Name of the analyte (default: \code{"Synthetic_Analyte"}).}

\item{formula_mu}{Character string or expression defining the trend for mu over age \code{x}.}

\item{formula_sigma}{Character string or expression defining the trend for sigma over age \code{x}.}

\item{formula_nu}{Character string or expression defining the trend for nu (for BCCG, BCPE, BCT).}

\item{formula_tau}{Character string or expression defining the trend for tau (for BCPE, BCT).}

\item{prop.path}{Numeric between 0 and 1. Proportion of pathological cases per age step (default: 0).}

\item{ill_factor}{Alias for \code{prop.path} for backward compatibility.}

\item{mu_factor_ill}{Numeric. Mean shift magnitude added to simulate pathological values.}

\item{lod}{Optional numeric. Limit of Detection threshold. Values below LOD will be flagged or processed.}

\item{seed}{Optional integer. Seed for random number generation (default: NULL).}
}
\value{
A data.frame with standard laboratory columns: \code{AGE_YEARS}, \code{AGE_DAYS},
\code{VALUE}, \code{IS_BELOW_LOD}, \code{ID}, \code{SEX}, \code{STATION}, and \code{ANALYTE}.
}
\description{
Generates synthetic age-dependent laboratory dataset across continuous age ranges
using normal, log-normal, or Box-Cox transformation distributions (BCCG, BCPE, BCT).
Supports adding pathological cases and censoring values below Limit of Detection (LOD).
}
